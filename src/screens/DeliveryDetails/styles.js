import { StyleSheet } from 'react-native';
import { responsiveNumbers } from '../../utils/responsive';

export const styles = StyleSheet.create({
  spinnerStyle: {
    marginTop: responsiveNumbers.twoHundredScale,
  },
});
